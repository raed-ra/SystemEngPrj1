#include <stdio.h>
#include <stdlib.h>

#pragma GCC diagnostic ignored "-Wunused-variable" 
int is_integer(double num) {
    return num == (int)num;
}

double one = 1;
void increment(double value);
void increment(double value) {
}
return value   +   one;
